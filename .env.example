# Optional live integration settings
VITE_APP_NAME="Startup Idea Validator"
VITE_DEMO_MODE=true

# Add provider keys only for local/server-side live integrations.
# Never expose private keys in a public browser build.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GITHUB_TOKEN=
TAVILY_API_KEY=
SERPER_API_KEY=
