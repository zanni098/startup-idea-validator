interface:
  display_name: "Startup Idea Validator"
  short_description: "Repo-specific patterns and workflows for startup-idea-validator"
  default_prompt: "Use the startup-idea-validator repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true